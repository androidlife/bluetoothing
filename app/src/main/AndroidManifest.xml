<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wordpress.laaptu.bluetooth">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Bluetooth Permissions -->
    <!-- To use bluetooth, no runtime permission required -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <!-- To make bluetooth discover other devices in periphery, no run time permission required -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <!--
   From Android >= 6.0, this permission only enables bluetooth device discovery,
   run time permission required
   http://stackoverflow.com/questions/34966133/android-bluetooth-discovery-doesnt-find-any-device
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test.MainActivity" />
        <activity
            android:name=".test.connect.ConnectActivity"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity
            android:name=".test.ChatActivity"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity
            android:name=".test.refactor.ui.ChatActivity"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity
            android:name=".test.TestActivity"
            android:configChanges="layoutDirection" />
        <activity
            android:name=".test.refactor.ui.ConnectActivity"
            android:label="@string/title_activity_connect"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>